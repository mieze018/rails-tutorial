<% unless comment.archived? %>
  <p>
    <strong>Commenter:</strong>
    <%= comment.commenter %>
  </p>

  <p>
    <strong>Comment:</strong>
    <%= comment.body %>
  </p>
    <p>
    <%= link_to "削除", [comment.article, comment], data: {
                  turbo_method: :delete,
                  turbo_confirm: "コメントを削除します。よろしいですか?"
                } %>
  </p>
<% end %>
